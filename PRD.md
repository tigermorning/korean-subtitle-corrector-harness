# 개인 에이전트 하네스 PRD
상태: D01~D10 CHOSEN/DEFERRED 완료. 1단계(읽기 전용) 구현 착수 가능.

## 제공된 제품 목표
사용자가 자연어로 작업을 요청하면 모델이 필요한 도구를 선택하고, 내 프로그램이 이를 검사·실행하여 결과를 다시 모델에 전달한다. 자료를 읽는 범용 작업과 코드를 수정하고 검증하는 코딩 작업을 모두 다룬다. 사용자는 진행 상황과 실패 이유를 확인할 수 있다.
PRD(Product Requirements Document)는 무엇을 누구를 위해 만들고 어떤 결과를 완료로 볼지 기록한다. 구현 방법의 모든 세부사항을 미리 고정하는 문서는 아니다.

## 학생이 완성할 사용자 이야기
D01 사용자/업무: CHOSEN (2026-09-08)
- 나는 한국어 자막 교정기(`korean-subtitle-corrector`)의 개발자로서, 실사용에서 나온 오교정 사례가 38파일 10,784줄의 규칙 코드 중 어느 규칙 탓인지 `파일:줄` 근거와 함께 분류하고 싶다.
- 지금 불편한 점: 그 분류가 전부 사람 손이다. 저장소 히스토리 309커밋에 "실사용 감수"가 8차까지 반복되고 오탐지 수정 15건·정정 16건이 쌓였지만, 라벨된 평가 사례는 71건(`examples/eval/heldout.jsonl` 41 + `작업자자료.jsonl` 30)에서 멈춰 있다. `docs/RETROSPECTIVE.md`에 "실제 검증은 매번 새 텍스트를 손으로 돌려보는 방식에 의존했다"고 적혀 있다. 고치면 다른 게 깨지는 회귀는 원인이 아니라 이 처리량 병목의 결과다.
- 실제로 성공했다고 판단할 결과: 사례를 입력하면 원인 규칙 후보를 실재하는 `파일:줄` 근거와 함께 돌려준다. 같은 모델·같은 프롬프트로 도구 접근만 뺀 대조군(A)과 비교해 정확도와 근거 유효율이 올라간다.
- 실습용 자료: 해당 저장소 안의 `.srt` 9개(범용 자료 작업)와 `subtitle_corrector/` 파이썬 소스 38파일(코딩 작업). 이미 사람이 정답을 아는 라벨 71건을 채점 기준으로 쓰되, 정답 라벨 자체는 모델 입력에 넣지 않는다.

## 공통 요구사항
| ID | 제품이 해야 할 일 | 학생이 구체화할 부분 |
|---|---|---|
| R01 | 작업 요청을 받고 진행·완료·실패를 구분한다 | 사용자 입력/UI, 작업 식별과 상태 표시 |
| R02 | 모델→도구 요청→검사→실행→결과 반환을 직접 구현한다 | 반복 횟수·시간 제한과 오류 정책 |
| R03 | 허용한 자료를 읽고 근거를 확인할 수 있는 결과를 만든다 | 첫 도구, 작업 폴더, 결과 형식 |
| R04 | 코드 변경을 보여 주고 제한된 테스트로 검증한다 | 변경 확인, 승인, 테스트 실행 대상 |
| R05 | 권한 없는 경로·승인 없는 변경을 막는다 | 승인 단위, 거절·취소 시 동작 |
| R06 | 실행 기록과 세션을 구분하고 이어갈 범위를 설명한다 | 저장 항목, 재시작 후 복원 여부, 민감정보 제외 |
| R07 | 모델 제공자와 제품 내부 계약을 분리한다 | 첫 제공자·모델, 도구 호출 지원과 변환 |
| R08 | 고정 10문항으로 기준 측정과 변경 후 측정을 수행하고 결과를 비교한다 | 모델·예산·반복 수·코드 버전, 원본 결과·점수·실험 보고서 |

## 학생의 선택
D02 플랫폼: CHOSEN — Python CLI. 화면 실행 위치와 도구 실행 컴퓨터가 같은 로컬 프로세스.
D03 언어: CHOSEN — Python. 대상 저장소와 harness-lab 예시가 모두 Python이라 테스트 실행·이식 비용이 낮다.
D04 첫 provider: CHOSEN — 수업 제공 OpenAI API 키. 도구 호출 정식 지원. 키는 환경변수로만 읽고 문서·소스에 넣지 않는다. 구체 모델명은 구현 시 확인해 기록한다.
D05 첫 작업과 도구 범위: CHOSEN — 작업 폴더는 `~/Documents/korean-subtitle-corrector` 원본. 1단계는 읽기 전용(파일 읽기 + 내용 검색) 두 도구로 오교정 사례의 원인 규칙을 추적한다. `.venv/`(1.6GB)·`.git/`·`__pycache__/`·`.pytest_cache/`는 제외 패턴으로 코드에서 막는다. 2단계에서 같은 폴더 안 쓰기와 pytest 실행을 추가한다.
D06 승인 방식: CHOSEN — 읽기·검색은 자동 실행. 쓰기는 대상 경로와 diff, 명령 실행은 실제 명령줄과 작업 디렉터리를 보여 주고 건별 승인. 거절하면 그 호출만 실패로 모델에 돌려주고 반복은 계속, 취소하면 반복 전체를 중단한다.
D07 세션과 저장: CHOSEN — 세션마다 JSONL 파일 한 개에 반복·도구 호출·결과를 기록한다. 재개는 구현하지 않으며 재시작하면 새 세션임을 표시한다. API 키와 키가 담긴 환경변수 값은 기록에서 제외한다.
D08 추가 provider: CHOSEN — OpenAI와 Ollama 두 어댑터를 두고 전환·비교를 지원한다. 어댑터 경계는 첫 구현부터 두되 Ollama는 OpenAI 경로가 끝까지 통한 뒤 붙인다. 모델별 도구 호출 지원 여부는 구현 시 실측해 기록한다.
D09 확장 기능: DEFERRED — 취소·진행 이벤트·동시 실행은 2단계(쓰기·pytest) 이후로 미룬다. 그 전까지 넣지 않는다.
D10 하네스 소스 위치: CHOSEN — 이 저장소의 `harness/` 폴더. 대상 저장소와 물리적으로 분리되어 작업 폴더 경로 검사가 자기 소스를 포함하지 않는다.

## 최초 범위와 제외 범위
최초 범위: Python CLI 하네스. OpenAI 어댑터 하나로 모델을 직접 호출하고, 도구 요청을 파싱·검사·실행한 결과를 다시 모델에 넘기는 반복을 소스로 구현한다. 도구는 읽기 전용 둘(파일 읽기, 내용 검색). 작업 폴더는 `~/Documents/korean-subtitle-corrector` 한 곳이며 제외 패턴을 코드에서 검사한다. 반복 횟수 상한과 종료 조건을 코드에 둔다. 세션마다 JSONL 기록을 남긴다. 첫 작업은 오교정 사례의 원인 규칙을 `파일:줄` 근거와 함께 찾는 것이다.
후속 범위: 같은 폴더 안의 쓰기와 pytest 실행 + 건별 승인과 경로·명령 검사(R04·R05), Ollama 어댑터와 provider 전환(D08), R08 고정 10문항의 기준 측정과 변경 후 측정.
이번 버전에서 제외: 세션 재개 — 상태 복원 범위를 따로 정의해야 해서 첫 연결을 늦춘다(D07). D09 확장 기능(취소·진행 이벤트·동시 실행) — 아직 미결정이라 확정 전에는 넣지 않는다. 대상 저장소 수정 — 1단계에 쓰기 도구를 아예 두지 않아 원본을 망칠 경로를 없앤다.
기본 제외: 공개 원격 제어 서비스, 임의 시스템 전체 접근, 비밀키 공유. 원격 접속은 별도 설계 없이는 추가하지 않는다.

## 완료 기준
ACCEPTANCE.md의 공통 시나리오에 실제 증거를 남긴다. 실제 모델 한 종류에서 범용 작업과 코딩 작업을 확인하고, 오류·거절도 검증한다. 추가 provider와 저장 재개는 선택한 범위에 따라 확인하거나 제외 사유를 쓴다. 모델 응답의 품질과 프로그램의 도구 실행 성공은 별개로 평가한다.

## 벤치마크 실험
Docker 없이 실행하는 Terminal-Bench Pro 로컬 이식판의 고정 목록으로, 원본 easy 2·medium 4·hard 4를 사용한다. 실행 가능한 Python 예시와 로컬 평가 연결·점수 모니터는 별도 harness-lab 자료로 제공한다. 자신의 구현을 같은 로컬 평가 규격에 연결하거나 예시를 이해하고 개선한다. 높은 점수 자체가 완료 조건은 아니며, 기준 측정과 가설에 따른 변경 후 측정의 실제 결과를 제출한다. 오류·미완료를 분모에서 제외하지 않고 개발용 채점기 진단과 학생 모델 성능을 구별한다.

난이도는 고정된 upstream task.toml의 분류를 유지한다. 경로와 Python 실행을 로컬로 이식한 결과이며 공식 컨테이너 점수가 아니다. 더 다양한 환경을 일관되게 재현할 때 Docker를 사용할 수 있으나 기본 과제에서는 요구하지 않는다.
