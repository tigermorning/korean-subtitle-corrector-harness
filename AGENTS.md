# 설계 협업 지침
이 프로젝트는 학생이 제품 결정을 내리고 AI와 자신의 에이전트 하네스를 구현하는 실습이다.

## 대화 시작
먼저 README.md, PRD.md, DECISIONS.md를 실제로 읽는다. 인터페이스/검증을 다룰 때 INTERFACES.md와 ACCEPTANCE.md, 구현 전에 IMPLEMENTATION_PLAN.md도 읽는다. 문서 이름만 보고 내용을 추정하지 않는다.
이미 선택된 결정은 재질문하지 않는다. 아직 OPEN인 결정 중 의존성이 앞선 것부터 한 번에 1~3개 질문한다. 첫 대화는 사용자의 실제 작업과 실행 환경을 묻고, 답을 받은 뒤 언어·provider·권한·저장 방식으로 이어간다. 선택지를 설명하되 선택은 학생에게 맡긴다. 추천과 합의를 구분한다.
사용자가 답하면 DECISIONS.md의 상태·선택·이유와 PRD.md의 관련 요구사항을 함께 갱신한다. 모호한 답은 한 구체적 예로 확인한다. 답하지 않은 핵심 선택을 임의로 확정하거나 시간이 지났다고 동의로 간주하지 않는다. 문서 수정은 이 설계 협업에 포함된다.

## 구현으로 넘어가는 조건
D01~D07의 첫 구현에 필요한 범위를 학생과 정한다. D08/D09의 추가 기능은 DEFERRED로 둘 수 있다. 상세한 내부 함수명 같은 일반 구현 선택은 AI가 정하고 기록해도 된다.
학생이 합의한 범위로 구현하라고 요청하면 해당 범위의 구현을 진행한다. 선택 기능까지 모두 답해야 시작할 수 있다고 막지 않는다. 범위·권한·외부 비용이 달라지는 새 결정만 질문한다. 준비가 끝나면 한 문단으로 합의 내용을 확인하고 첫 구현을 제안한다. 학생의 최초 'PRD를 같이 구체화하자'는 구현 요청이 아니다.

## 구현과 검증
구현 전 INTERFACES.md, ACCEPTANCE.md, IMPLEMENTATION_PLAN.md를 합의 범위에 맞춰 채운다. 개발에 사용하는 Claude Code/OpenCode와 학생 제품의 모델 실행부를 구분한다. 제품은 직접 모델을 호출하고, 도구 요청을 검사·실행하고, 결과를 반환하는 반복을 소스로 구현한다. SDK를 통신에 사용하는 것은 가능하지만 전체 반복을 기존 에이전트 CLI/Agent SDK에 위임하지 않는다.
처음에는 읽기 도구 하나로 끝까지 연결한다. 이후 승인받은 쓰기와 제한된 테스트 실행을 추가하여 범용 작업과 코딩 작업을 검증한다. 정해진 언어·웹 프레임워크를 강제하지 않는다.
실제 키를 문서나 코드에 넣지 않는다. 파일 접근 범위, 승인, 종료 조건은 코드에서 검사한다. 작업 폴더 제한을 운영체제 샌드박스라고 부르지 않는다. 실행하지 않은 검증은 NOT_RUN으로 남긴다. 실패를 성공으로 바꾸거나 모의 응답을 실제 모델 실행처럼 기록하지 않는다.

## 평가 과제
R08의 고정 10문항 기준 측정과 변경 후 측정, 점수·원본 결과·소스 제출을 구현 계획에 포함한다. harness-lab의 문서와 실제 명령을 확인하고 학생이 선택한 구현을 연결한다. 선택한 제공자의 연결이나 로컬 실행 자원이 부족하면 실제 측정이 미완료임을 기록한다. 공식 정답이나 숨은 채점 코드를 에이전트 입력에 넣어 점수를 올리지 않는다.
