# 설계 결정 기록
OPEN: 미결정 / CHOSEN: 학생이 선택 / DEFERRED: 학생과 합의하여 후속으로 미룸.
추천은 선택이 아니다. 상태를 바꿀 때 학생 답과 이유를 기록하고 PRD/계약/검증을 함께 갱신한다.

| ID | 결정 | 상태 | 선택과 학생 답 | 이유·영향 |
|---|---|---|---|---|
| D01 | 사용자/업무 | CHOSEN | 한국어 자막 교정기 개발자 본인. 실사용 오교정 사례가 어느 규칙 탓인지 `파일:줄` 근거와 함께 분류하는 일 (2026-09-08) | 히스토리 근거로 선택했다. 309커밋 중 "실사용 감수"가 8차까지 반복되고 오탐지 수정 15건·정정 16건이 쌓였는데, 라벨된 평가 사례는 71건(heldout 41 + 작업자자료 30)에서 멈춰 있다. 대상 규칙은 38파일 10,784줄. `docs/RETROSPECTIVE.md`가 "실제 검증은 매번 새 텍스트를 손으로 돌려보는 방식에 의존했다"고 기록한다. 즉 회귀가 나는 것은 원인이 아니라 라벨링 처리량이 사람 손에 묶인 결과다. 이 작업은 사례 1건당 검색→읽기를 여러 번 반복해야 하므로 하네스의 반복·검사 구조가 실제로 필요하다. |
| D02 | 플랫폼 | CHOSEN | CLI (2026-09-08) | INTERFACES.md의 명령 계약(`python -m harness ask/show`)이 이미 CLI 전제로 작성돼 있어 그대로 확정. 로컬 웹 서버는 추가하지 않는다. 화면 실행 위치와 도구 실행 컴퓨터가 같은 로컬 프로세스로 일치한다. |
| D03 | 언어 | CHOSEN | Python (2026-09-08, 3강 플랫폼·언어 대화로 재확인) | 대상 저장소 korean-subtitle-corrector가 Python이라 테스트 실행·모듈 조회가 같은 생태계에서 끝난다. harness-lab 예시도 Python이라 비교·이식 비용이 없다. `run_pytest`를 같은 런타임 subprocess로 실행해 언어 간 통신이 필요 없고, OpenAI tool calling SDK도 Python 지원이 안정적이다. TypeScript는 로컬 웹 화면이 필요해질 때, Rust는 소유권 규칙까지 배울 이유가 있을 때로 미룬다. 복합 언어는 지금 이유 없어 채택하지 않는다. |
| D04 | 첫 provider/모델 | CHOSEN | 수업 제공 OpenAI API 키 (2026-09-08) | tool calling 정식 지원이라 R02 반복·도구 요청 파싱이 안정적이다. 키는 환경변수로만 취급하고 문서·소스에 넣지 않는다. 구체 모델명은 구현 시 확인해 기록한다. |
| D05 | 최초 도구/폴더/테스트 범위 | CHOSEN | 작업 폴더 = `~/Documents/korean-subtitle-corrector` 원본, 1단계는 읽기 전용 (2026-09-08) | 사본 대신 원본을 쓰되 1단계에 쓰기 도구를 아예 두지 않아 망칠 경로가 없다. 제외 경로: `.venv/`(1.6GB), `.git/`, `__pycache__/`, `.pytest_cache/`. 폴더 제한만으로는 부족하고 제외 패턴을 코드에서 검사한다. 2단계에서 같은 폴더 안 쓰기와 pytest 실행을 추가한다. |
| D06 | 승인 방식 | CHOSEN | 읽기·검색은 자동, 쓰기와 명령 실행만 건별 승인 (2026-09-08) | 반복이 끊기지 않으면서 되돌리기 어려운 행동만 막는다. 승인 화면에는 쓰기의 경우 대상 경로와 diff, 실행의 경우 실제 명령줄과 작업 디렉터리를 보여 준다. 거절 시 그 도구 호출만 실패로 모델에 돌려주고 반복은 계속한다. 취소는 반복 전체를 중단한다. |
| D07 | 세션/저장 방식 | CHOSEN | 세션마다 JSONL 파일 한 개로 기록, 재개는 구현하지 않음 (2026-09-08) | 이 작업은 사례를 반복 실행하는 성격이라 실행 기록이 곧 R08 원본 결과물이자 A/B 비교 입력이 된다. 재개는 상태 복원 범위를 따로 정의해야 해서 첫 구현을 늦추므로 뺀다. 재시작하면 새 세션이 시작된다는 점을 사용자에게 표시한다. API 키와 키를 담은 환경변수 값은 기록에서 제외한다. |
| D08 | 추가 provider | CHOSEN | OpenAI + Ollama 로컬 모델, 전환·비교 지원 (2026-09-08) | R07의 제공자/내부 계약 분리를 실제로 검증하려면 어댑터가 둘 이상이어야 한다. R08 실험에서 모델을 바꾼 측정도 가능해진다. 어댑터 경계는 첫 구현부터 두고, Ollama 어댑터는 OpenAI 경로가 끝까지 통한 뒤 추가한다. 모델별 도구 호출 지원 여부는 구현 시 실측해 기록한다. |
| D09 | 확장 기능 | DEFERRED | 2단계(쓰기·pytest) 이후로 미룸 (2026-09-08) | 취소·진행 이벤트·동시 실행 모두 첫 연결에 필요하지 않다. 2단계까지 끝난 뒤 실제로 걸리는 것을 보고 정한다. 그 전까지는 넣지 않는다. |
| D10 | 하네스 소스 위치 | CHOSEN | `harness-design-kit/harness/` (2026-09-08) | 설계 문서와 소스를 한 폴더에 둔다. 대상 저장소(`korean-subtitle-corrector`)와 하네스 저장소가 물리적으로 분리되어, 작업 폴더 경로 검사가 자기 소스를 우연히 포함하지 않는다. |

## 결정 변경
| 날짜 | 변경한 결정 | 변경 이유 | 영향받는 요구·계약·검증 |
|---|---|---|---|
| 2026-09-08 | D06: `write_file`이 기존 파일만 바꾸던 것을 새 파일 생성까지 허용(상위 폴더는 있어야 함, 새 디렉터리는 여전히 안 만듦) | R08 벤치마크(agent-terminal-benchmark, 고정 10문항)를 내 하네스로 실제 실행(A11)해 보니 10문항 전부가 "새 출력 파일 생성"을 요구해서 0/10이 나왔다. 모델 성능이 아니라 이 좁은 제약이 원인임을 실행 기록(예: sudoku 문항이 2회 반복 만에 "solution.txt가 없어서 못 만든다"고 스스로 답하고 끝남)으로 확인한 뒤 11강 개선 실험으로 바꿨다 | `harness/tools/write_file.py`(preview/run 로직, `_digest`가 "없음"과 "빈 문자열"을 구별), `INTERFACES.md`의 write_file 계약, `tests/test_write_tools.py`(새 테스트 3개), `ACCEPTANCE.md` A03·A12 |
| 2026-09-08 | D05: 도구 범위에 `run_python`(작업 폴더 안 기존 `.py` 파일 하나 실행, 셸·인라인코드 불가) 추가 | write_file 개선(위 행) 후 재측정해도 이진 점수는 0/10 그대로였다. sudoku 문항 세션 기록을 보니 모델이 `sudoku_solver.py`를 실제로 새로 쓰는 데는 성공했지만, 그 스크립트를 실행할 도구가 없어 `solution.txt`가 끝내 안 만들어졌다. 벤치마크가 기본 제공하는 참조 에이전트(같은 모델, `run_python` 있음)는 같은 조건에서 1/10을 풀어 "도구만 있으면 최소 일부는 풀린다"는 대조군을 확보한 뒤 추가했다 | `harness/tools/run_python.py`(신규), `harness/tools/__init__.py`(READ_WRITE_TOOLS 등록), `harness/loop.py`(WRITE_PROMPT_SUFFIX), `INTERFACES.md`의 도구 표, `tests/test_run_python.py`(신규 7건), `ACCEPTANCE.md` A12(재측정) |
