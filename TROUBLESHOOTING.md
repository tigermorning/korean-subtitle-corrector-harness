# 내 하네스의 실패 진단
코드가 길어졌을 때는 전체를 다시 만들기보다 명세와 실행이 처음 달라진 지점을 찾습니다. 이 안내는 특정 언어·프레임워크의 정답 소스를 요구하지 않습니다.

## 작은 요청으로 재현
민감하지 않은 실습 파일에 `모임 시작: 오후 3시`를 적습니다. 만든 하네스에 그 파일의 시작 시간을 묻습니다. 기대 결과는 파일 읽기 도구가 실제 실행되고, 읽은 결과를 받은 모델이 오후 3시라고 답하는 것입니다. 문서나 채팅에 정답을 미리 넣어 도구 없이 맞힌 경우와 구별합니다.

최소 재현은 같은 문제를 일으키는 가장 작은 입력과 실행 방법입니다. 자료와 도구를 줄이되 실패 원인을 없애 버리지 않았는지 확인합니다. 한 번에 한 조건만 바꾸면 어떤 변경이 영향을 줬는지 알기 쉽습니다.

## 증상별 확인 순서
| 증상 | 확인할 경계 | 작은 검사 |
|---|---|---|
| 모델 응답 없음 | 제공자 연결 | 키 값 없이 설정 유무·모델 이름·오류 종류를 확인. 인증 오류와 연결 실패를 분리 |
| 응답은 있지만 도구 요청 없음 | 도구 설명 전달과 모델 지원 | 실제 요청에 도구 설명이 포함됐는지 확인. 모의 도구 요청을 넣어 하네스 후반부가 동작하는지도 별도로 확인 |
| 도구 요청을 받았으나 실행 안 됨 | 해석·인자 검사·도구 등록 | 등록된 이름, 필수 인자, 경로 오류를 비교. 오류를 무시하거나 모든 경로를 허용하지 않음 |
| 읽기는 됐지만 답이 틀림 | 도구 결과의 다음 모델 요청 연결 | 작은 가상 파일의 내용이 해당 요청 결과로 전달됐는지 확인. 전달이 맞다면 답변 품질 실패로 구분 |
| 동일 도구가 반복됨 | 대화 갱신·결과 연결·종료 | 결과 반환 누락을 확인하고, 정상 연결 상태에서도 모의 반복 요청을 넣어 한도 종료 검사 |
| 승인 거절 후에도 변경됨 | 승인→쓰기 실행 순서 | 같은 변경을 거절하고 전후 파일 비교. 거절 경로에서 쓰기 함수 호출이 차단되는지 검사 |
| 테스트 실패를 성공으로 표시 | 종료 코드→작업 상태 | 실패하는 테스트 결과를 주고 실패 표시 확인. 모델의 성공 주장만으로 완료시키지 않음 |
| 세션 후속 요청이 앞 내용을 모름 | 세션 선택·저장·대화 복원 | 같은 세션 ID의 새 작업인지 확인. 실행 중 유지와 앱 재시작 후 복원을 분리 |

모의 응답은 하네스 코드의 특정 경로를 재현하는 데이터입니다. 모의 도구 요청이 성공해도 실제 모델이 도구를 선택한다는 증거는 아닙니다. 코드 경로를 고친 뒤에는 실제 모델로 같은 작업을 다시 확인합니다.

## 실패 기록 양식
아래 빈칸을 실패 하나에 대해 채웁니다. 관련 명세 ID는 PRD·INTERFACES·ACCEPTANCE에서 찾습니다.

- 관련 요구/검증 ID:
- 사용한 프로그램 실행 방법, provider/모델:
- 사용자 요청과 작은 실습 입력:
- 기대 결과:
- 실제 결과:
- 마지막으로 성공한 단계:
- 최초로 어긋난 단계 또는 아직 모르는 경계:
- 오류 종류와 필요한 실행 기록:
- 확인할 가설:
- 가설을 구분할 작은 검사:
- 변경한 코드 또는 명세:
- 동일 입력 재실행 결과:
- 함께 확인한 정상/거절 사례:

키·토큰·실제 개인 파일은 적지 않습니다. 오류 메시지에도 민감한 정보가 들어갈 수 있으므로 공유 전에 확인합니다. 필요한 기록이 없다면 먼저 단계 이름, 도구 이름, 검사 결과, 결과 전달 여부, 종료 이유를 남기도록 구현합니다.

## 실제 기록 예 (2026-09-08)
1단계·3단계를 모의로 통과시킨 뒤, 처음으로 실제 모델과 실제 터미널로 돌렸을 때 걸린 사례입니다.

- 관련 요구/검증 ID: A04 (승인 거절 시 파일 불변)
- 사용한 프로그램 실행 방법, provider/모델: `py -m harness ask "..." --root fixtures/spacing_rules --provider openai --allow-write`, OpenAI `gpt-4o-mini`
- 사용자 요청과 작은 실습 입력: `fixtures/spacing_rules/rules.py`의 정규식 결함을 고쳐 달라는 요청. 비대화(non-TTY) 실행이라 승인은 자동 거절되도록 되어 있음
- 기대 결과: `write_file` 요청이 거절되고, "[n] write_file 거절됨 — ..." 같은 로그가 찍힌 뒤 프로그램이 정상 종료
- 실제 결과: 로그를 찍는 도중 `UnicodeEncodeError: 'cp949' codec can't encode character '—'`로 프로세스가 죽음 (종료 코드 1이 아니라 처리되지 않은 예외)
- 마지막으로 성공한 단계: `write_file` 도구 실행과 거절 판정까지는 끝남 — `session_end` 이벤트는 세션 JSONL에 기록됨
- 최초로 어긋난 단계 또는 아직 모르는 경계: `loop.py`가 화면에 사람이 읽을 로그를 남기는 지점(`emit` → `print`). 세션 기록(JSONL)과 화면 출력은 서로 다른 경로인데, 화면 출력 쪽만 죽었다
- 오류 종류와 필요한 실행 기록: `UnicodeEncodeError`. 어떤 문자가 문제인지(`—`, em dash)와 콘솔 코드페이지(cp949)가 트레이스백에 이미 있었음
- 확인할 가설: (1) 모의 테스트가 이 경로를 안 잡은 이유는 `print()`가 실제 OS 콘솔로 안 나가서다 (2) Windows 콘솔 코드페이지가 기본이 cp949라 UTF-8 전용 문자가 낄 때마다 재발할 것이다
- 가설을 구분할 작은 검사: `harness/loop.py`와 `harness/__main__.py`에서 ASCII 범위 밖 문장부호(em dash, 화살표 등)를 검색 → `emit(f"... 거절됨 — ...")` 한 곳에서 발견
- 변경한 코드 또는 명세: `harness/__main__.py`의 `main()` 시작부에서 `sys.stdout`/`sys.stderr`를 `encoding="utf-8", errors="replace"`로 재설정. 문자를 지우는 대신 콘솔 인코딩을 고정해, 앞으로 다른 문장에 특수문자가 들어가도 같은 종류의 크래시가 안 나게 함
- 동일 입력 재실행 결과: 같은 요청을 다시 실행 → 크래시 없이 정상 출력, `write_file` 두 번째 시도까지 로그가 찍히고 거절 메시지도 화면에 정상 표시됨(`evidence_sessions/20260908T062825Z-a11b62b6.jsonl`)
- 함께 확인한 정상/거절 사례: 수정 후 `py -m pytest tests -q` 재실행 → 51 passed, 1 skipped(회귀 없음). A01(정상 읽기)도 재실행해 한글이 화면에 깨지지 않고 그대로 나오는 것도 함께 확인

## AI와 원인 좁히기
먼저 다음처럼 요청합니다.

> 이 실패 기록과 우리 명세를 비교해서 처음 어긋난 곳을 찾아 줘. 원인을 확인할 작은 검사부터 해보자.

AI의 설명은 가설일 수 있습니다. “아마 모델 문제”라는 답을 받으면 어떤 기록으로 그렇게 판단했는지 확인합니다. 결과 반환 누락인지 모델 해석 오류인지에 따라 수정할 곳이 다릅니다.

원인이 확인되면 다음처럼 요청합니다.

> 확인한 원인만 고치고 같은 입력으로 다시 검사해 줘. 정상 읽기와 거절 동작도 유지되는지 확인하자.

새 기능을 붙이거나 모델을 바꾸기 전에 기존 실패가 해소됐는지 확인합니다. 합의한 기대값을 구현의 잘못된 출력에 맞춰 낮추지 않습니다. 요구 자체를 바꾸기로 결정했다면 변경 이유를 DECISIONS에 남기고 PRD·인터페이스·검증도 함께 갱신합니다.
